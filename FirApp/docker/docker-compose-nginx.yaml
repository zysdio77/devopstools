services:
  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
    restart: always
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
      - ./ssl:/etc/nginx/ssl
      - ./data/android:/usr/src/dist/android:ro
      - ./data/ios:/usr/src/dist/ios:ro
      - ./data/plist:/usr/src/dist/plist:ro
    depends_on:
      - app

  app:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    restart: always
    volumes:
      - ./template.plist:/usr/src/dist/template.plist
      - ./data/android:/usr/src/dist/android
      - ./data/ios:/usr/src/dist/ios
      - ./data/plist:/usr/src/dist/plist
    depends_on:
      mysql:
        condition: service_healthy

  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: 123456
    security_opt:
      - seccomp:unconfined
    volumes:
      - ./data/mysql:/var/lib/mysql
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    restart: always
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 3s
      timeout: 3s
      retries: 10
